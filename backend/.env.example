# Opcional: sem esta chave o backend usa o Ollama local (sem custo).
# Com a chave, usa o OpenRouter (modelo gratuito por padrao).
OPENROUTER_API_KEY=
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2
